if(board != answerBoard){
            int userRow = 2;
            char userCol = 'A';
            int userNum = 1;
            board = updateBoard(board, userRow, userCol, userNum);
            printBoard(board);
        } else {
            System.out.println("YOU WIN!");
        }
